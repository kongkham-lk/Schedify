<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme -->
    <style name="Theme.Schedify" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your theme here -->
        <item name="android:windowSplashScreenBackground">@color/splash_screen_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/app_icon</item>
        <item name="android:windowSplashScreenIconBackgroundColor">@color/splash_screen_background</item>
        <item name="colorPrimary">@color/primaryColor_Btn</item>
        <item name="colorOnPrimary">@color/primaryColor_Text</item>
        <item name="android:textColor">@color/white</item>

    </style>
</resources>
